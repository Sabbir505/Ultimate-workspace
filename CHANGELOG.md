# Relay Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

> **Naming note:** "Relay" is the product name on every surface as of the rename completion (`7f6952b1`, 2026-09-05): user-visible strings (window title, `package.json` name, `tauri.conf.json` `productName`, `<title>`, sidebar/banner/HTML strings), the Rust crate (`relay`, lib `relay_lib`), the bundle identifier (`dev.relay.app`), the sidecar binaries (`relay-browser-mcp`, `relay-automation`), the MCP server identifiers (`relay-browser`, `relay-tools`), the NSIS installer filename, the mobile app (`Relay Mobile`, `com.relay.mobile`), and the Windows scheduled-task name (`RelayAutomations`). Existing installs migrate transparently — see the [0.5.0] Changed entry and `docs/ai-context/RELEASE.md` for the full compatibility matrix. The only pre-rebrand values kept on purpose are the E2E pairing crypto constants (`conduit-e2e-relay-*`), which are protocol-anchored on both desktop and phone.

---

## [Unreleased]

## [0.5.0] — 2026-09-17

**A feature release covering 345 commits since 0.4.2** — relief from single-chat, single-session amnesia. Relay now runs up to six chats side by side in one window, agents can see and message each other's sessions, a persistent memory survives across chats, artifacts improve themselves from their own run telemetry, and a companion pet keeps the workspace company. Alongside: an in-process local read-aloud, a rebuilt browser agent core, a notification center, auto model routing, a first-run wizard, and the architecture debt paid down (the event seam, DB lock scopes, and the 4k-line chat store all carved up).

### Added
- **Split-pane chat workspace — up to six chats in one window** — a binary pane tree (`paneTree.ts`) where every pane splits left/right or top/bottom (hard cap of six), every gutter drag-resizes independently, and a session lives in at most one pane (opening or dragging the active chat falls the main pane back to another chat, so a live turn renders in exactly one view). Drag a session row onto any pane edge to open it there with a half-pane outcome preview; moves relocate the chat and collapse its old pane; too-small panes refuse to split. Sidebar clicks collapse to a single view and remember the layout; clicking a pane chat restores the layout focused on that pane. Per-pane message buffers (`paneBuffers` + a shared `bufferWriteBack` helper) put send/cancel/done/error rows in whichever buffer displays the session; `TurnNavigator` and the ActivitySteps subagent chips became pane-scoped.
- **Companion pet** — a pixel-sprite companion (`generate_pet_sprites.mjs` manifest pipeline; cat / axolotl / robot sheets, hats, idle/walk/celebrate/doze/zoomies rows) with a pure mood state machine (`src/state/pet.ts`: watching / work / celebrate / concerned / doze / happy, plus XP, levels and hat unlocks), dual homes (above the sidebar search and above the composer), a popover panel (rename, species, hats, visibility, off), and an Appearance settings section. It reacts to real app state — `usePetEvents` bridges chat streaming, pane working state, automation runs, the notification center and user presence — with level-up parties, drag-to-move between homes, test reactions, milestones, a focus buddy, a budget mood, and reduced-motion awareness.
- **Session Mesh — cross-session awareness, messaging and spawning** — closes the "no cross-session state" gap: agents see, consult and delegate to every other Relay chat. `list_sessions` / `read_session` / `search_sessions` (peer registry, summaries, recent turns, FTS over all chats' messages), `message_session` (point-to-point mailbox — answers return as the tool result or a follow-up turn, never lost between sessions), and `spawn_session` (a real sidebar-visible session on any engine, depth-capped). Backed by `session_summaries` + `session_mail` tables, a `chat_sessions.origin` column, a `session_fabric` runtime (registry-block injection into the built-in system prompt, the harness bundle and a per-turn hint for resumed CLI chats) and a lazy summarizer.
- **Persistent user memory across chats** — background extraction worker (secrets-filtered, cursor-idempotent, spawned post-turn), a consolidation judge (ADD / UPDATE / DELETE / NOOP over similarity-gated candidates, with contradictions bi-temporally superseded and a `memory_ops` audit log for every decision including NOOPs), dual-axis scoring (LLM-judged importance 1–9 with an over-rating cap; evidence-derived confidence with read-time staleness decay), hybrid retrieval (nomic cosine + FTS5 bm25 min-max fused, MMR, project scoping, confidence floor), a reflection pass that synthesizes cited insights, and tiered injection (always-on profile block ≤500 tok, per-turn `remembered_context` ≤800 tok fenced and provenance-tagged, plus a `memory_recall` tool). `memory_save` / `recall` / `forget` tools in both wire formats, a Settings → Memory browser (search/filter/edit/retire/add/purge/export/toggle) over nine IPC commands, and evidence propagation that flags unbacked memories when their source message or session is deleted. Memory is now one human-readable document with the pipeline hardened around it (doc merge can't lose facts, tool writes keep provenance).
- **Self-improving artifacts (P0→P2)** — the design in `SELF_IMPROVING_ARTIFACTS.md`, shipped end to end: immutable artifact version history (`improve_artifacts` / `improve_versions` / `improve_channels`), execution telemetry (`improve_runs` / `improve_feedback`) with runs opened per skill invocation and closed by the turn lifecycle (applied / failed+code / abandoned, edit-to-fork marking runs corrected), a persistent goal-loop runtime (`loop_sessions`), improvement sweeps that hand artifacts with ≥3 failed/corrected runs in 7 days a reflective proposal from the active chat model, eval packs harvested from corrected runs, champion-vs-challenger evaluation (deterministic assertions plus blind order-randomized LLM-judge scores) behind a regression gate that fails any candidate breaking a case the champion passed, canary promotion with shadow windows (≥10 clean runs promotes, a dirty or expired window auto-rolls back and marks the proposal stale, two rollbacks permanently demote the artifact to manual), per-artifact autonomy tiers, an `improve_events` audit log, a Settings → Improvements panel, and automation runs linked into the loop.
- **Document Design Layer — plan-first DOCX/PPTX/PDF generation** — a layer between the model and the render engines so generated documents look designed instead of hand-coded: `tokens.json` as the single source of truth (7 themes, type scales, spacing, chart palettes, faces) read by the TS compilers, the HTML print CSS (replacing the hand-written `BASE_CSS` in `pdfprint.rs`) and the bundled Python helper; a 13-layout deck catalog with CJK-aware text-fit budgets; a document IR (sections/blocks) with prose budgets; deterministic compilers (DeckPlan → PptxGenJS, DocPlan → docx, DocPlan → print-engine HTML) each with L2 invariant checks (bare hex, single save, no `dLblPos`, token fonts); a `plan_document` tool so the model authors a plan rather than code, with L1 validation errors steering in-turn patching; and layered QA (plan checks, compile invariants, L4 render probes over the LibreOffice PDF via pdf.js). Harnesses can reach the same flow through `plan_document` / `revise_document` exposed over the conduit-tools MCP.
- **Local read-aloud (TTS) — Kokoro-82M in-process** — no cloud, no API key, no sidecar to manage: Kokoro runs through sherpa-onnx inside the app. A play button on every assistant answer (fed by the existing `plainText` memo, so think blocks and tool markup are never voiced), read-aloud on text/markdown/code/json/csv artifacts, a global transport bar (pause / prev / next / stop + sentence counter) that survives pane switches, chunked synthesis with prefetch so playback starts on the first sentence, optional auto-read scoped to the visible session, and voice auditioning from the picker. It reads docs, decks and PDFs aloud, cleans up voice symbols and abbreviations, and (with the CUDA runtime updater below) can run on GPU.
- **Composer voice dictation, rewritten** — partial transcription splices into the draft itself rather than ghost text: silence gaps (~0.77 s) commit each segment so words survive as speech continues, the live partial re-renders in place, the final full-clip pass replaces it with polished text, cancel undoes inserted words, and hand edits during dictation append instead of being clobbered. Dictation stop is snappy (stale-request cancellation, one partial in flight, sidecar exit kill).
- **Browser agent core v2** — 14 new MCP tools (`find`, `fill_form`, `select_option`, `press_key`, `batch` with halt-on-failure, `read_console`, `read_network`, `list_tab` / `switch_tab` / `new_tab` / `close_tab`, `zoom`, `print_to_pdf`, and the previously unreachable `screenshot` now advertised), actions returning a compact ref-stable snapshot with a canonical stale-ref protocol, a trust layer, per-project browser profiles, an ephemeral MCP port with a discovery file (was a fixed 7681), a URL-scheme allowlist (`javascript:` / `data:` blocked) with `PermissionRequested` auto-deny, and tab titles + favicons from the injected bridge.
- **Notification center** — a title-bar bell before the side-panel toggle with a white unseen-count badge (red when errors are unseen) and a glass panel whose rows jump to the chat, pane or view that produced them, plus Read all / Clear and Escape/outside-click dismissal. One funnel (`lib/notifyCenter.ts`) records every important event (completed / error / crash / approval / automation / alert) in a persisted, capped history that survives restarts, while the OS toast and sound are gated by DND, the notify-sound setting and app focus; unfocused completions get a calm chime. Toasts bear the Relay identity and the panel caps at four rows.
- **Harness update checker + one-click updates** — compares each installed harness CLI's own `--version` against the npm registry's latest dist-tag (1 h cache, force re-check available) and offers an Update button in Settings → Agent harnesses with progress and a post-install re-probe, plus a quiet once-per-boot check that surfaces in the bell. Installs resolve through the real PATH walk (Windows `PATHEXT` included), so a native copy shadowing the npm shim updates through its own tool (winget, `claude update`, `kimi upgrade`, `opencode upgrade`) while npm-managed binaries keep using `npm install -g`.
- **First-run onboarding wizard** — the approved 5-step flow: Meet Relay (real brand glyphs, 3D CSS cube hero, directional step transitions, header logo + N/5 + clickable progress dots), Choose your path (experience cards), Pick an agent (live `listHarnesses` probe with skeleton shimmer, Ready/Install states, a first-class Local Model row deep-linking into the Model Market, Re-scan), Workspace (real folder picker with an honest name/path/isGitRepo preview, deferable), and Set your defaults (permission segmented control writing the new session posture, model tiles persisting the provider default with an inline key form, and a task panel that genuinely starts the first chat).
- **Auto model routing + reasoning-effort control** — an "Auto" picker entry routes each built-in chat turn to an available cloud model with fail-over and cost bias, so users stop guessing which provider works, has credit or is rate-limited (local GGUF and harness CLIs are deliberately not auto candidates — single-slot sidecar and no CLI auth/credit visibility). Every committed pick persists (`chat.last_selection`) so new chats stop asking again each launch — which also fixed the duplicate-session race it exposed. The reasoning-effort slider now maps to real model controls, appears on every pane that carries it, and extends to CLI harness chats.
- **Multiple named API endpoints per provider kind** — endpoint ids became instances: a kind's first endpoint keeps the bare kind id (existing data and active-provider logic unchanged), later ones get `<kind>-<suffix>`, a `chat.instance_ids` registry lists them, and the settings rail and composer's Direct API section list one entry per endpoint under its assigned name (Add API now offers every kind every time). Protocol dispatch resolves the kind from the id, so sends, model listing, one-shots and context gating work per endpoint without touching their storage.
- **Token-efficiency pass** — Anthropic prompt caching with `cache_control` breakpoints (tools → system → newest message) on both the tool loop and the non-tool path, plus a strip-and-retry fallback for gateways that reject the field; OpenRouter `anthropic/*` models get the same marks through the OpenAI wire format while other vendors never receive unknown fields; local-docs retrieval now appends as the LAST message so the request prefix stays byte-stable (injecting it at the head had been silently invalidating every provider's automatic prefix cache, every turn). Tool specs were capability-gated (browser interaction and memory tools by default off the surface) and token-dieted: 45.0k → 36.5k chars of tool surface per request.
- **Automations matured** — five automation tools (`list` / `create` / `update` / `delete` / `run_automation_now`) registered in the provider-agnostic registry and rendered into both wire formats, so every provider including local GGUF can use scheduling instead of answering "I can't schedule things"; run prompts are compiled from the spec and run unattended; harness sessions get automation CRUD with real harness errors instead of empty responses; the run log opens into the chat view and streams live; past-run durations tick live while in flight; and a Stop button kills the harness process tree and records a neutral `stopped` status (no failure banner, toast or email) while releasing the overlap guards (runs owned by the run-while-closed binary report back as not stoppable rather than pretending).
- **Themes and typography** — seven curated presets ship in the Theme Gallery as one-click cards (Nord, Tokyo Night, Catppuccin Mocha, Rosé Pine, Dracula, Gruvbox Dark, Solarized Light), copied into the user's gallery on apply so they persist, export and deselect exactly like imported themes, plus UI-font (Space Grotesk, Inter, IBM Plex Sans, Roboto, editorial serif, system) and code/terminal mono (Space Mono, JetBrains Mono, Fira Code, Cascadia Code, system) pickers.
- **Production-quality diagrams** — mermaid 10.9.6 → 11.17.2 with flowcharts defaulting to the lazily-imported ELK layout engine, deliberate layout config (node/rank spacing, curves, sequence margins), a new `--diagram-*` palette mapped across flowcharts, sequence, state, ER, notes and edge labels (custom themes can override; re-init keys on live token values so theme swaps restyle diagrams), a real font stack (the old `var(--font-sans)` token never existed, so text had been falling back to the webview default), and export controls with a 1–4× PNG scale picker and transparency.
- **Markdown interactivity** — citation markers (`[1]`, `[1,2]`, `(1,2)`) resolve against the turn's Sources section or a generated `.md` artifact as chips with hover previews that open the source in the built-in browser pane, generated tables became a rounded full-width card with hover Copy (TSV) and CSV download (in chat and in artifact previews), plus text zoom and selection actions.
- **New logo and app-wide branding** — the final Relay mark rolled out everywhere with a transparent background, including the update-available modal (real mark instead of a generic refresh icon) and the mobile app icons.
- **App zoom** — `Ctrl` `+` / `-` / `0` zooms the whole app.
- **Native build updaters for the pinned CUDA builds (the harness-updater shape, applied to binaries)** — the four pinned native artifacts this app downloads and executes (whisper.cpp server CPU build, whisper.cpp CUDA build, llama.cpp CUDA server, sherpa-onnx CUDA TTS runtime) now have the same check→Update flow harness CLIs have: each installer stamps a `build-version.json` marker into its install dir, `check_build_updates` compares the marker against the version the app pins (the same SHA-verified constants the installers pull), and rows surface in a unified **"Server builds"** card — one row per build with a version-delta chip (`vX · current` / `vX → vY` / Not installed) and a single Install/Update action — placed in Settings → Local Models → My Models (llama) and → STT & TTS (whisper CPU/CUDA, TTS GPU), plus a once-per-open bell check (`checkAndNotifyBuildUpdates`, deduped per version in localStorage, deep-linking to the Speech panel) mirroring `checkAndNotifyHarnessUpdates`. Highlights:
  - **One-click CUDA llama-server** (`llama_install_cuda`, id `llama-cuda-server`) — the previously manual step of dropping a CUDA build for local-model GPU offload. Installs the pinned official `llama-b10985-bin-win-cuda-12.4-x64` prebuilt (SHA-256 verified at pin time against the real asset) into `bin/llama-cpp-cuda`, stamps the marker, and points `localModels.llamaServerPath` at it; `resolve_llama_server_binary` slots the managed build ahead of the bundled CPU sidecar (an explicit user path still wins). A CUSTOM CUDA build already in use (e.g. a self-built drop found by the drive scan) is probed (`--version` + `ggml-cuda` presence) and reported on the row with a note that updating installs the official prebuilt and points the app at it — the custom build is never modified. The row's "latest" is the newest `b`-tagged llama.cpp release (one GitHub API GET per check, offline falls back to the pin).
  - **One-click CUDA whisper build** (`stt_install_cuda`, id `stt-whisper-cuda`) — the pinned `whisper-cublas-12.4.0` asset from the SAME release tag the CPU zip uses, SHA-256 verified at pin time against the real asset, extracted flat into `bin/whisper-cpp-cuda` where `cuda_binary()` already resolves it — it bundles its own CUDA 12 runtime DLLs, so no CUDA toolkit install is needed.
  - Existing installers gained a `force` flag so an update re-downloads over a live install (a running sidecar is stopped first — Windows won't overwrite a running image); the shared download/verify/extract pipeline moved to one module (`commands/pinned_zip.rs`). Update rows flip optimistically to "current" (`markBuildUpdated`) exactly like the harness rows.
- **Pinged stall detection + reconnect ladder for lost provider connections** (`chat/reconnect.rs`) — the flat 60 s stall watchdog no longer fails a turn by the clock alone. While a round is silent the reader PINGS the endpoint (`GET {base}/v1/models`; any HTTP status counts as reachable, so only DNS/connect/TLS/timeout reads as "down", and a 401 or a 404 from a proxy still proves the network is up): reachable means the request is probably just slow — long thinking, big prompt eval — so the wait extends to 90 s instead of dying at 60 s, and an endpoint that stops answering fails the read immediately instead of waiting the window out. `send` then re-dials the SAME model and request up to 10 times, each attempt spaced by a ping, and reports progress as a "Reconnecting… (n/10)" line under the assistant bubble (a muted pill in the slot the hover action bar occupies) while the partial the user was already reading stays on screen. A retry that lands RESTARTS the answer — the live buffer is dropped and handed to `supersededPartial`, so a ladder that ultimately gives up still persists the text the user watched (`onError` falls back to it) and a successful one can't append the new answer to the old. Safety: the ladder only runs while the turn has produced no side effects (`TurnPerf::tools_ran == 0`) — replaying a round that already wrote a file or ran a command would do it twice, so a stall after a tool keeps the plain fail-the-turn path; and a non-connection refusal (429, 5xx, dead key) is handed straight back to the fail-over/health classifier untouched. Metrics are not rewound: the discarded attempt was billed, so its tokens and time stay in the turn's totals. Subagent rounds keep the flat deadline (no ladder behind them), and harness-CLI turns are unchanged.
- **Three new harnesses: Pi, Omp, CommandCode** — the harness registry grows from three CLIs to six, each with a full adapter (spawn / resume-by-id / login / session-id capture / usage scraping / diff-approval prompts, all conservative per the adapter contract). Flags verified against upstream sources: Pi (`@earendil-works/pi-coding-agent`) resumes via `pi --session <id>`, auth is the in-TUI `/login`; Omp (`@oh-my-pi/pi-coding-agent`) resumes via `omp --resume <id>`, login is `omp setup`; CommandCode (`command-code`) resumes via `--resume <id>`, login is `commandcode login`. Adapter presentation order is now deterministic (`ADAPTER_ORDER`) so Settings/agent-picker rows stop reshuffling as the registry grows. New harnesses work in interactive panes (spawn/resume/login), the sidebar harness select, the agent picker rail (monogram glyphs), and the one-click npm install; the headless chat/automations turn backend is unchanged (a chat turn on the new ids reports "no headless chat backend yet", same as kimi in automations).
- **Harness parity with built-in chat** — gallery MCP servers now reach harnesses (enabled installed servers are translated to stdio entries in claude/kimi `mcp.json` and `opencode.json`, with name collisions skipped and empty env blocks omitted), flagless adapters get the instructions bundle prepended to their first turn, and harness context gained memory, the date and artifact location guidance.
- **Artifacts are listable and discoverable** — a read-only `list_artifacts` tool (newest-first from the 30-day retention, with kind, date and absolute path, filename-substring filter and limit, available under every permission mode) registered in both provider schemas and the system prompt, so agents list instead of guessing where an artifact lives; built-in chat file tools also surface their files in the gallery, and artifact proposals now report honest confidence (intent confidence × spec completeness).
- **One-time Install from the automations failure banner** — when an automation's last run failed and its harness CLI isn't installed on this device, the banner's "Run again" button becomes "Install" (npm install -g, with progress + error toasts); once the install lands and the re-probe sees the binary, the button flips back to "Run again". The plain-language spawn-failure hint now points at the Install affordance; the banner hint names the missing CLI.
- **`automationHarnessInstall.test.ts`** — covers the harness-missing decision (registered-but-not-installed → Install; provider/local agents never match) and the hint copy.
- **`get_capabilities` — in-process availability introspection** — a dedicated read-only tool that answers "which connectors / MCP servers / skills are available?" from the turn's live `ToolCaps` (attached connectors with their tool lists, attachable connectors, attached MCP-gallery servers grouped by wire name, attachable servers, enabled built-ins) instead of a config file. Agents no longer have a reason — or permission — to spawn a Terminal just to inspect connector/MCP availability. Three surfaces:
  - **Built-in chat + subagents** — advertised in both provider schemas (always on, no approval); the report embeds the terminal lifecycle contract so the model reads live limits, not stale prompt text.
  - **Harness CLIs (Claude Code / Kimi / OpenCode)** — the `conduit-tools` MCP server gains `get_capabilities` (`tool_op` mapping, `tools/list` schema, relay whitelist); it reports the app-level state (connected connectors, installed/enabled MCP-gallery servers, in-app browser surface, skills). The harness environment preamble tells CLIs to use it instead of running `claude mcp list` in their TUI.
  - **Anti-probe guard** — the `run_shell` dispatch refuses `mcp list`-style availability probes (MCP mention + probe verb — `list`/`ls`/`status`/`which`/`where`/`--version` — high-precision, so `claude mcp add`, `grep mcp src/`, `git status` all pass) with a redirect message pointing at `get_capabilities`. The attach-manifest prompt segment and the `run_shell` description state the same contract.
- **Terminal process lifecycle rules** — every model-started shell now belongs to exactly one of four classes, defined once in `chat::tasks` (module doc + constants) and enforced in code, with the `run_shell` tool description teaching them:
  - **Foreground** (default): runs to completion, output returned inline, hard 120s ceiling (`SHELL_FOREGROUND_TIMEOUT_SECS`); an explicit `timeout_secs` may only shorten the run.
  - **Temporary**: explicit `timeout_secs` (5–3600, auto-clamped) — the engine kills the process exactly at the deadline (foreground **or** background) and reports the timeout; no cleanup needed.
  - **Background**: `run_shell(background: true)` returns a task id immediately; output streams via `get_task_status`; killed by `cancel_task` or app exit (`kill_on_drop`).
  - **Long-running**: background without `timeout_secs` (dev servers, watchers, long installs) — no engine deadline, but the model owns the `cancel_task` cleanup.
  - `run_shell_to_completion` takes its timeout as a parameter; the background `shell_task` enforces the Temporary deadline in its select loop. The old timeout notice that pointed at the nonexistent `start_shell` tool now points at `background: true`.
- **Research mode overhaul (P0 foundation + trust layer)** — The research system (`RESEARCH_MODE_IMPROVEMENTS_RESEARCH.md`) upgrade, end to end:
  - **Multi-engine keyless search** — `web_search` now merges DuckDuckGo HTML, Mojeek (independent index), and Wikipedia with per-engine health reporting (`engine health: … FAILED` degrades honestly instead of "no results"). The dead DuckDuckGo Instant Answer API (returns empty payloads since long ago) was removed — it masked real outages as empty result sets.
  - **BYO-key search engines** — Settings → Web Search: Serper (Google), Tavily, or Brave replace the keyless SERP engines when configured (keys stored via the generic settings store; Wikipedia stays as a supplement). Brave-only payloads are never cached, per Brave's storage terms.
  - **Readability extraction + Jina Reader fallback** — `fetch_url` extracts articles with `dom_smoothie` (Readability.js port; byline/publish-date surfaced), falls back to the tag-stripper for non-article pages, and falls back to the keyless `r.jina.ai` reader when a fetch is blocked or the target is a PDF (PDFs are now readable). Text budget per read raised 12 KB → 32 KB. SSRF guards run before both paths.
  - **Research caches** — SQLite `search_cache` (12 h TTL) and `page_cache` (7 day TTL, canonical-URL keyed, tracking params stripped) so re-reading a source never re-hits the wire; `research_queries` history powers a repeat-query nudge ("each research query should explore NEW ground") and a per-session search audit trail; `reset_source_ledger` clears both.
  - **Citation-integrity lint** — End of every research turn, the generated report is mechanically checked against the source ledger (zero model calls): orphan citations (`[n]` pointing at sources never read), unused ledger sources, uncited sentences, and weak attribution (cited sentence shares too little vocabulary with the stored verbatim excerpt — the "quote absent from the cited article" failure class). Verdict persists in `citation_reports` and rides to the UI as `chat:citation-report`, rendered as a trust strip above the composer (green/amber/red).
  - **Evidence-sufficiency gate** — New `check_sufficiency` tool: before synthesizing, the model must declare per-sub-question status (≥2 independent sources, opposing views looked for); the tool returns SUFFICIENT / NOT SUFFICIENT with the gaps spelled out.
  - **Source metadata** — `add_source_note` gains optional `publisher` / `publishedAt` (from page metadata); the ledger schema carries both, and the research prompt now requires explicit conflict handling: prefer fresher/more authoritative sources, state both positions with dates when genuinely ambiguous, never average into a false consensus.
  - **Scope phase** — Research scaffolding now opens with a brief + optional (max 2) clarifying questions for ambiguous asks before any search spend.
  - **`/research` runs on every engine** — the protocol now rides the CLI appendix for harness sessions instead of being a built-in-chat-only path, and takes over the slash token properly on selection.
- **Research P1/P2 backlog** — the remaining improvements from `RESEARCH_MODE_IMPROVEMENTS_RESEARCH.md`:
  - **Citation-chip verification colors** — chips cited by the end-of-turn lint render amber (weak attribution) or red (orphan source); verdicts reach the bubbles via `chat:citation-report` payload numbers and a React context, so the memoized markdown cache needs no re-keying.
  - **Async citation-precision sampler** — when the heuristic lint flags weak attributions, a single background model call re-judges up to 12 flagged claim/excerpt pairs with the session's own provider; "supported" verdicts clear flags live (a refined report re-emits + persists), "unsupported" confirms them. Skipped for local models (no extra context spend).
  - **"Fix citations" repair action** — flagged reports show a button on the citation strip that pulls the stored lint detail and sends a RARR-style repair instruction: re-cite from the ledger, re-read the flagged source, or drop the claim; regenerate the artifact.
  - **Compact ledger mode** — `get_source_ledger(mode:"compact")` returns the claim index without verbatim excerpts for small-context/local models with large ledgers.
  - **Deep-tier fan-out guidance** — broad multi-area research may spawn 2–4 `task` subagents (one per sub-question cluster) recording into the shared ledger and returning compressed findings; the lead dedupes, resolves conflicts, and still passes the sufficiency gate.
  - **Citation-quality trend** — `citation_quality_trend` aggregates lint counts over recent reports: flag-ratio drift after a prompt/model change is the regression signal.
  - **Anchor-based weak attribution** — the heuristic now measures containment over a claim's load-bearing tokens (numbers, dates, proper nouns) instead of all content words; real-run over-flagging (44/63 "weak" on a clean report) drops to a sane rate while unrelated-claim misattribution still trips it.
- **Context compaction and honest context windows** — the three unrelated stories (local compaction, no cloud story, blind harnesses) replaced by one window-resolution chain and a shared engine:
  - **Per-model context-window registry (P0)** — The flat 500k ceiling is retired for the meter: a per-model registry (mirrored backend/frontend) resolves real windows (Claude 200k, GPT-5 400k, GPT-4.1 1M, Gemini 1M, …) with the 500k figure as fallback; OpenRouter's live `context_length` is no longer capped. Cloud/harness meters poll a live backend estimate (system + history + tool schema) and take the max with the provider-counted last turn, so warn/crit can actually fire before an overflow. The hover breakdown is estimated per category from real content (no more fabricated 15/70/10/5 split).
  - **Cloud context compaction (P1)** — Cloud/API sessions now compact like local ones: pre-send estimate against a per-model window triggers the same pin+summarize engine, with the session's OWN provider writing the summary; persisted `[compacted context]` rows supersede folded turns. A provider context-overflow rejection is now classified (`context_overflow`), auto-compacted, and retried once instead of dying as a raw 400 banner.
  - **Overflow error classification (P0)** — `chat:error` carries a machine-readable `code`; provider phrasings across Anthropic/OpenAI/OpenRouter/llama-server/Google map to `context_overflow`, with actionable banner copy instead of a raw provider blob.
  - **"Compact now" (P1)** — Manual forced compaction for the active session from the context-meter panel (`chat_compact_now`), cloud and local.
  - **Context recovery UI (P2)** — The `[compacted context]` marker gains a "show folded turns" disclosure: the raw turns a summary folded away are fetched from the DB on demand (the summary is lossy; the rows are the restorable source) via `list_compacted_messages`.
  - **Structured summary schema (P2)** — The shared summarization prompt now produces the 9-section schema (primary request/intent, decisions, files & code, errors & fixes, all user messages, pending tasks, current work, next step, pointers), tuned recall-first — errors are kept as evidence, never discarded. Per-message head+tail trimming protects the summarizer budget from single oversized turns.
  - **Map-reduce summarization (P4)** — Local compaction no longer silently drops the aged-out turns that don't fit the summarizer budget: they're chunked, map-summarized, and folded into the main call; summary output cap raised 1024 → 2048.
  - **Summarizer override + rebuild-from-raw (P4)** — `chat.local_gguf.compaction_summarizer = "cloud"` routes local summaries through a configured cloud provider; rebuild-from-raw re-derives each new summary from the ORIGINAL folded turns instead of stacking summary-on-summary.
  - **Harness context observability (P3)** — Claude Code's own auto-compact (`compact_boundary`) persists a boundary marker and refreshes the meter; `/compact` and `/microcompact` are offered in the composer for harness sessions (forwarded verbatim to the CLI).
  - **Harness primer upgrade + resume-gap fix (P3)** — The engine-switch context primer now carries a cloud-summarized digest of the turns that exceed its (raised) 32k-char tail budget; a stale `--resume` id that kills the CLI before any turn activity is detected, dropped, and the next send replays the primer instead of resuming blank. The primer also replays DB history into fresh CLI sessions, so a mid-chat engine switch keeps the conversation.
- **Mobile companion upgrade** — app lock (biometric/passcode via `expo-local-authentication`) gating the app after backgrounding (an unlocked phone is a remote shell for the desktop), deep-link pairing (`relay://connect` URLs from the desktop QR/share flow, with liberal token-fragment parsing and host normalization), Expo push notification registration over the paired socket so approvals, turn completions, automations and budget alerts reach a backgrounded phone (plus haptics), and a chat UI overhaul. The desktop side gained a backend-owned art import dialog, pairing tokens in the OS keychain, family-aware cache pricing, partial-stream persistence on quit, a backend budget timer, and a shared auto-route + assistant-row constructor with mobile.

### Fixed
- **Four audit waves, fully remediated** — 60 findings (2026-09-06, seven audit agents across frontend, backend, mobile, scripts and configs), the full-project audit (2026-09-10 — security, hangs, data correctness, UI defects), 145 findings (2026-09-14 codebase audit) and the final 3 high / 16 medium / 24 low (2026-09-17). Representative fixes across the waves: a conditional hook in `AutomationRunTable` that crashed the app on the first run row (plus an app-level ErrorBoundary), a DB-move that wrote `storage.dbDir` to the new connection and could clobber an existing non-empty `relay.db`, model-download destination containment, a `cmd.exe` injection gate on `--model` for Claude spawns, one-shot user confirmation before a Relay-created automation can run headless full-auto (prompt-injection hardening), legacy raw-token mobile pairing removal, per-session map leaks on chat delete, stale-session `/create` targeting, and timeline memo churn per streaming token. Every finding's status is annotated in its audit doc.
- **The DbState self-deadlock that froze the app** — `preview_scope_roots` resolved the artifacts dir through `dispatch::artifacts_dir(app)`, which locks the same non-reentrant `DbState` mutex its callers already held, so artifact previews (and every command queued behind the global DB mutex) wedged forever — a frozen window on the main thread, silent IPC death once the async workers were all parked. Fixed at the source, with the graph/status commands offloaded to `spawn_blocking` and the main-thread stalls removed.
- **Harness respawn could double-lock the session stdin cell** — the first Claude send after a respawn self-deadlocked and froze the whole window.
- **`Alt` release dropped the window into Win32 menu mode**, stalling WebView2 IPC for 10–30 s (push-to-talk dictation).
- **CommandCode model list no longer sticks at zero** — three stacked cache/timeout bugs made a transient discovery failure permanent for the whole app run: the agent picker's pane cache kept a "ready but empty" CommandCode pane forever (the one revalidation trigger checked only for a MISSING `effortOptions` field, and commandcode's empty `[]` was defined, so an empty pane never re-probed and had no Retry button); the backend's 30s TTL cached empty probe results unconditionally; and a probe that hit the 15s timeout discarded whatever the CLI had already printed. Now: an empty-but-ready harness pane is re-probed on the next picker open, the empty pane in the picker gets a "Retry discovery" button, `list_harness_models` doesn't cache empty results (the next open re-runs the probe instead of serving "zero models" for 30s), and a timed-out probe returns the partial output the CLI managed to print (a CLI that emitted its listing and then stalled still yields its rows; junk lines can't parse as models). Picker labels are derived from model ids rather than CLI descriptions.
- **Metrics and cost are real again** — non-tool OpenAI-family requests now send `stream_options.include_usage` and keep reading past `finish_reason: stop`, so turns no longer recorded NULL tokens ($0 in rollups) on the composer HUD; usage parsing captures cached and reasoning tokens; per-turn usage and provider-derived cost are correct across harnesses and built-in chat; composer token metrics count uncached input only; harness usage is scraped honestly rather than estimated.
- **Context-meter tooltip follows provider and model switches** — the hover breakdown was cached per chat SESSION only, and the backend resolves it against the session's CURRENT provider/model (exact `/tokenize` counts for a loaded local model, per-category estimates for cloud). Pick a local model, hover (tooltip shows the sidecar's counts and window), switch the session to a hosted provider → the tooltip kept serving the local model's numbers for the rest of the session; hover before the sidecar finished loading (backend returns nothing) → "Breakdown unavailable" was pinned forever, even after the model was ready. The cache key now includes provider + model + local-ness (a switch invalidates it), and an unavailable result retries on the next hover instead of caching the failure.
- **CommandCode turns report real TTFT (and speed) again** — the CommandCode event handler ignored `model_request_start`, so the generation window only opened at the FIRST delta — microseconds before the first token was emitted — and TTFT collapsed to a confident "0 ms" on the composer HUD (and tok/s degraded with it whenever the delta stream didn't match). The window now opens when the request actually goes out to the provider, matching the Claude handler's `message_start` behavior. Tool results also strip the CLI's `<usage>` trailer, and the double-reply after answering a question is gone.
- **Full-auto by default everywhere** — new built-in and local-GGUF chat sessions are created `workspace_write` + `full_access` (no per-action approval cards), matching the harness CLIs, which all run headless turns unrestricted (`--dangerously-skip-permissions` for claude, kimi prompt auto-approve, opencode `--auto`, pi `--approve`, omp `--auto-approve`, commandcode `--yolo --tools-all`). The pi-lineage turn flags are now explicit rather than relying on print-mode defaults. read_only / plan / auto_edit stay one switch away in the mode menu, and legacy rows still fail closed to manual. Ejecting a harness back to built-in now applies the real full-auto policies (silently — no confirmation modal — since full-auto is the default) instead of a label-only reset that left older sessions on per-action approvals.
- **Agent picker no longer freezes while harness models load** — `list_harness_models` was a sync Tauri command (main thread), and the new pi/omp/commandcode discovery live-probes each CLI (1–3s node/bun cold start per probe), so opening the agent picker froze the whole window for seconds. It is now async with the probe work on a blocking thread pool plus a 30s TTL cache, matching `list_harnesses`/`list_acp_agents` ("opening the agent menu must never freeze the window").
- **Harness turn permission model verified + first-send flash removed** — pi/omp print modes auto-approve tool calls (verified live: a file-creating headless turn executed with no approval flag; pi's `--approve` is project-trust for local extensions/settings and is deliberately left at the safe "ignore" default), CommandCode runs with `--yolo`; documented at the spawn site. The per-send "… is starting up…" chat status is now Kimi-only — for pi/omp/commandcode it was noise, since the CLI is already exercised at agent/model selection time.
- **Headless resume verified on-device** — pi (`--session <id>`) and omp (`--resume <id>`) both continued prior sessions by the captured CLI session id and recalled context (codeword round-trips); the pi session store lives at `~/.pi/agent/sessions/<cwd-slug>/` keyed per project.
- **Headless chat + automations for Pi, Omp, and CommandCode ("true streaming")** — the per-turn engine now speaks all three CLIs' JSON event protocols, verified live on-device: pi/omp stream `-p --mode json` (`message_update`/`text_delta` deltas, thinking blocks, tool execution markers, cumulative usage, session header for cross-turn resume); CommandCode streams `-p --output-format json` (`{"type":"event"}` frames + final `result` line, liberal delta matching with a `finalText` catch-up so the reply lands even if event names drift). Model discovery reads each CLI's live listing (`pi --list-models` table, `omp models --json`, `commandcode --list-models` with the `(default)` marker) plus pi's `~/.pi/agent/{settings,models}.json` (default model, custom-provider endpoint); Pi and Omp (and CommandCode) now also appear in the automations/agent pickers with their official logos (pi.dev pixel-π, omp.sh glyph, commandcode.ai cmd symbol).
- **Fixed the Windows prompt transport for npm-shim harnesses** — the M12 env+wrapper design silently breaks for CLIs installed as npm `.cmd` shims: a batch-to-batch hand-off passes the RAW line to the shim, whose `endLocal … %*` tail re-expands it with delayed expansion OFF, so the `!CONDUIT_TURN_PROMPT!` placeholder reached the CLI as literal text (reproduced with an argv-dump shim; kimi/opencode were unaffected only because they ship native .exe binaries). pi/omp/commandcode turns now pipe the prompt to the child's stdin (verified live: exact prompts answered through the real cmd.exe wrapper), with the E-7 kill-the-tree contract on write failure. Oversized prompts and model switching on harness sessions were fixed in the same pass, and `/create` one-shots run `--bare` so they beat the 180 s timer.
- **Install flow is now self-verifying** — after `npm install -g` succeeds, `install_harness` polls the CLI's `--version` for up to ~15s (freshly written shims can fail their first probe while Defender scans), so the harness row flips to "installed" without a manual Re-check; if the CLI still doesn't run, the toast says so. omp additionally reports up front that its npm distribution requires Bun (the earlier "installed but still shows Install" symptom on this device was exactly this — Bun was missing; installing it made `omp --version` pass).
- **Settings "Re-check" now actually re-checks** — `list_harnesses` serves a 30s in-memory cache, and the button used to call that same cached command, so a harness installed or uninstalled out-of-band (manual `npm install -g`, PATH change) showed no change for up to 30s no matter how often you clicked. The command takes a `force` flag that bypasses (and refreshes) the cache; the Re-check button passes it, shows a "Checking…" state while the probe runs, and toasts instead of failing silently when the backend call errors. The in-app install flows (Settings + automations banner) also force the follow-up probe; boot and the agent picker keep the cheap cached path.
- **Security hardening** — the leaked OAuth fixture was purged, `connect-src` pinned, and the unscoped opener grant dropped; Hugging Face download tokens now attach only to `https://*.huggingface.co` URLs; artifact preview/open/download IPC endpoints enforce the same granted-roots containment as the model's file tools; `sanitizeSvg` neutralizes `@import` / remote `url()` / `position: fixed` in `<style>` blocks and inline style attributes before main-document injection; the whisper.cpp STT installer verifies a pinned SHA-256 before extraction; `turn_spec` fails the turn instead of falling back to an argv prompt when wrapper writing fails (closing the `cmd.exe` injection path); and native exec gates (`exec_gate.rs`) now put an OS dialog in front of the three surfaces that execute renderer-supplied strings — `spawn_shell` (exact command + working folder + secret-injection flag, Allow remembered per folder), MCP gallery custom servers (command + args, remembered per exact command line, deny fails closed on attach/connect/self-heal), and `set_llama_server_path` — while automation creation asks for confirmation once before a Relay-created automation can run headless full-auto.
- **Citation hover preview no longer overlaps text** — the source preview card renders through a portal at `document.body` with fixed positioning (flip below when out of room, viewport-clamped, opaque background, top z-layer): it used to be absolutely positioned inside the markdown flow, where virtualizer rows and markdown blocks' own stacking contexts painted over it and scroll containers clipped it.
- **Native link-title tooltips removed from chat markdown** — OS-positioned `title` tooltips painted over surrounding text unrecoverably; plain links open in the browser pane on click, citations have their rendered preview cards.
- **Citation check strip restyled** — proper CSS classes (glass tokens, single compact row, truncating hint, health dot green/amber/red, "Fix citations" pill); the inline-styled version wrapped into a tall multi-line block. The strip also dismisses when "Fix citations" dispatches its repair turn.
- **Chat and composer correctness sweep** — the transcript clamps to the measured tail (no void under the last turn), the selection toolbar appears only after the gesture ends and always above the selection, connector mentions render inline at text size and chip onto the bubble on send (`[Connected: …]`), `/create` commands no longer re-execute on the next send, slash selection consumes its token, the wheel over the composer dock scrolls the transcript, token pills no longer clip on scroll, metrics tooltips portal above their chips instead of clipping, paste-to-attach works, empty harness chats are no longer deleted on switch-away, markdown renders correctly across text kinds and languages, and `$5 and $10` no longer opens KaTeX.
- **Artifacts and previews** — the gallery dedupes and opens previews immediately instead of dead-clicking, artifact→chat switching works, the header's read-aloud button is visible, doc/PDF previews no longer clip, diagram lightbox pan/zoom was rewritten with inline-pane zoom added, and no-viewBox SVGs no longer crop.
- **Themes** — contrast audit fixes across all seven presets, primary buttons keep the built-in brand style everywhere and use `--accent-contrast` rather than hardcoded white, and light-theme glass shadows no longer read as dark overlays.
- **Pet polish** — the sidebar pet sits snugly on the search bar, the composer pet overlays the seam instead of wedging a 54 px gap, hats follow the pose (and their offset was upside down), a napping pet never teleports, zoomies render (the generator had assigned it a row past the sheet), the paw button actually toggles the panel closed, the panel's hover state is readable, and the panel is portaled so it isn't clipped.
- **Release and CI** — `latest.json` notes now carry only the released version's changelog section (the update banner had been rendering the whole raw CHANGELOG.md), and CI stages both sidecar placeholders before the first cargo build, since the main package's build script validates every `externalBin` during sidecar builds.

### Changed
- **Architecture debt paid down (zero behavior change, no wire-format changes)** — the four fixes from AUDIT_2026-09-13.md §1, plus a deep-tail carve campaign:
  - **One chat event seam** — `chat/stream_events.rs` now owns every `chat:token` / `status` / `done` / `error` / `artifact` emit, both agent worlds use the same typed payloads (harness-only cache keys are `skip_serializing_if` options, so built-in events stay byte-identical), the six approval-gate blocks collapsed into `run_approval_gate` (+ a blocking variant for harness reader threads), and the artifact insert+emit pair became one helper.
  - **Bounded DB lock holds** — `DbState`'s mutex now guards SQL only: chat/project export and import decompress, parse and zip outside the lock, `verify_project_path` and `is_path_allowed` canonicalize after release, and the rule is documented at the type. (`swap_chat_db_files` keeps its single documented hold.)
  - **Module splits** — `lib/ipc.ts` became a barrel over 19 domain modules + `ipcCore.ts`; `agent_sessions.rs` split into 13 modules; `chat/commands.rs` (6.8k lines) into nine domain modules; the 2.4k-line `BrowserManager` impl into four; `BrowserManager`'s injected-JS builders into `browser_js.rs`; `src/state/chat.ts` into `src/state/chat/` (11 slices over one flat zustand store, every existing import still resolving); Settings panels, ChatComposer (queue/notches/thinking chrome, modals, shared helpers, attachment plumbing, voice dictation, PCM utilities) and `AgentModelPicker` carved into focused modules with `ChatWelcome`, `ActivitySteps` and `useTranscriptScroll` extracted.
  - **Shared machinery** — one `ProviderSsePump` behind the built-in stream, the mobile chat loop and the OpenCode reader (`SseLineBuffer::with_cap`), one `llm_client` dispatch for every one-shot completion, shared tool-call execution in both tool loops, a shared subagent per-call tail, `NewChatMessage` replacing a 19-argument `add_chat_message`, `checked_send`/`checked_send_ctx` for status-checked requests, a resumable-download body pump (which also fixed a cancel deadlock), and frontend hooks/helpers (`useOcclusion` replacing nine popup-registration effects, `useTauriEvent`, `useCopyToClipboard`, `startPointerDrag`, `clearStreamState`, display-format consolidation, a single agent catalog and tool-segment model).
- **Complete Conduit→Relay rename with install migrations** — the rebrand now covers every internal surface, not just user-visible ones: the Rust crate (`relay`, lib `relay_lib`), the bundle identifier (`dev.relay.app`), the sidecar binaries (`relay-browser-mcp`, `relay-automation`), the MCP server identifiers (`relay-browser`, `relay-tools`), the mobile app (`Relay Mobile` / `com.relay.mobile`), and the Windows scheduled task (`RelayAutomations`). Existing installs migrate transparently on first launch: the app data dir renames itself (`%APPDATA%/dev.conduit.app` → `%APPDATA%/dev.relay.app`), keychain entries are read across generations and re-homed on delete, persisted legacy paths (`conduit.db`, `Documents/Conduit`, `~/Conduit/models`, `ConduitAutomations`) resolve their counterparts, AsyncStorage keys re-home on the phone, and the updater still accepts legacy `Conduit_` release assets. Only the E2E pairing crypto constants (`conduit-e2e-relay-*`) stay verbatim — they are protocol-anchored on both desktop and phone. ([7f6952b1](https://github.com/Sabbir505/Ultimate-workspace/commit/7f6952b1))
- **Chat code blocks restyled ChatGPT-style (fixed-dark in both themes)** — the copyable code block is now a near-black card (`#0d0d0d`, 10px radius) with light text and a quiet header: `</>` glyph + language name as written (no more ALL-CAPS), and an icon-only copy button (copy → check on success) instead of the "Copy" text pill. Light themes no longer bleach the block: the `--syntax-*` token palette is pinned to the dark Dark+ set inside `.chat-code-block` under light roots, and the highlighter resolves its colors from inside the block (a hidden probe element) instead of the document root — so dark themes and custom syntax themes keep working, but a light theme can't put near-black tokens on a near-black surface. The artifact pane's own doc-code chrome is untouched.
- **Chat workspace and composer restyled** — the sidebar retired the Projects tree for one flat inbox list (two-line rows: title + working spinner/relative time, then folder/project + git branch, and the provider/harness/local-model brand icon via a shared `sessionModelIcon` mapping) under the composer's liquid glass, with a borderless collapsed git rail showing a live status line (goal title / in-progress plan step / changes summary); the slash-command panel got woven-mesh glass, the tool-run badge became plain text with a shining title and latest-call badge, and plan/goal surfaces were fused.
- **Runtime performance** — three jank sources killed (transcript re-render storms, composer keystroke churn, artifact preview re-parse), plus a lag-free provider switch in the agent picker.
- **Compaction settings consolidated**: one "Compaction (advanced)" panel covers local (threshold, pin, summarizer, rebuild-from-raw) and cloud (enabled, threshold, pin) engines; cloud overflow-retry runs regardless of the master switch.
- **Frontend toolchain** — rustfmt 1.9 formatting adopted repo-wide (formatting-only commits) with the toolchain pinned so rustfmt is guaranteed present.

### Documentation & Maintenance
- **All documentation consolidated under `docs/`** (ai-context, architecture, audits, notes, research, superpowers), refreshed to match the implementation, with the superseded `PROJECT_OVERVIEW.md` and `conduit-architecture.html` dropped, and the audit/roadmap docs synced to the tree.
- **Verification grows teeth** — SSE-parse and provider-dispatch contract tests, round-parser delta-accumulation pins (mock runtime + scripted SSE), hermetic locale/timezone pins for the format tests, and regression guards around the terminal-cleanup unification.
- **Repo hygiene** — stale Playwright session screenshots/snapshots removed, one-shot codemod scripts dropped, and the git-pill harness entry tracked alongside the pet-lab and attachments harness pages.

---

## [0.4.2] — 2026-08-31

### Added
- **Boot splash + onboarding modal** — Brand logo shown during app startup; first-run local-model onboarding modal ([7321d69](https://github.com/Conduit-official/Conduit/commit/7321d69))
- **Parallel subagents with tools** — Subagents can now use tools in parallel; agent chips appear everywhere ([8476da2](https://github.com/Conduit-official/Conduit/commit/8476da2))
- **CDP execution layer** — Phase 1 of the Chrome DevTools Protocol layer for the in-app browser; subagent rounds raised to 100 ([6f5e452](https://github.com/Conduit-official/Conduit/commit/6f5e452))
- **Queued messages notch stack** — Steer, edit, delete, and drag-reorder pending messages before they're sent ([1c5bae6](https://github.com/Conduit-official/Conduit/commit/1c5bae6))
- **Subagent pane at chat-view fidelity** — Ordered segments, streamed thinking, and DiffCards in the agent pane ([d541f3d](https://github.com/Conduit-official/Conduit/commit/d541f3d))
- **Structured plan tracking** — Model-declared plans surface in chat with a Plan posture; harness-native modes ([19e63f2](https://github.com/Conduit-official/Conduit/commit/19e63f2))
- **Split chat view** — Full-fidelity second ChatView with glass session menu and a draggable split divider; tool panel docks beside the focused half ([e63de9c](https://github.com/Conduit-official/Conduit/commit/e63de9c), [5aed56c](https://github.com/Conduit-official/Conduit/commit/5aed56c), [c0e7b21](https://github.com/Conduit-official/Conduit/commit/c0e7b21))
- **Git file viewer with inline diffs** — Per-type file icons, click-to-expand inline diffs, Unstaged/Staged/All-branch/Last-turn filters, styled review cards with full markdown prose ([f4f9b21](https://github.com/Conduit-official/Conduit/commit/f4f9b21), [332ce72](https://github.com/Conduit-official/Conduit/commit/332ce72), [bcd7de1](https://github.com/Conduit-official/Conduit/commit/bcd7de1))
- **GitHub pull requests — liquid glass scope dropdown** — Scope selector uses liquid glass; GitHub API routes through git's proxy config ([bc605d0](https://github.com/Conduit-official/Conduit/commit/bc605d0))
- **Welcome screen refresh** — Time-aware greeting, icon cards, 500k context ceiling for cloud/harness meter ([2e85de8](https://github.com/Conduit-official/Conduit/commit/2e85de8))
- **Relay branding in agent prompts** — Agent self-identifies as Relay across cloud, local, and harness system prompts ([0b6e4e3](https://github.com/Conduit-official/Conduit/commit/0b6e4e3))
- **Full-fidelity document pipeline** — Browser-grade PDF via WebView2 + Paged.js, docx npm + PptxGenJS generation, pdf.js + docx-preview viewers; built-in `/docx`, `/pptx`, `/pdf` skills rewritten ([3c7b60e](https://github.com/Conduit-official/Conduit/commit/3c7b60e))
- **Fold same-tool rows** — Compact edit rows, sidebar more-popovers; 1M context default ([449183d](https://github.com/Conduit-official/Conduit/commit/449183d))

### Fixed
- **Audit sweep** — Relay auth fail-open, browser pane results, stream watchdogs, lifecycle wedges, data integrity ([7a5d6f3](https://github.com/Conduit-official/Conduit/commit/7a5d6f3))
- **Agent browser calls** — Reuse the existing Browser chip instead of stacking a duplicate; settings/skills/cost keep the chat mounted ([2495c8a](https://github.com/Conduit-official/Conduit/commit/2495c8a), [960bf75](https://github.com/Conduit-official/Conduit/commit/960bf75))
- **WebView2 controller ownership** — Own WebView2 controller via webview2-com; touch marshals through main thread; controllers created invisible then shown ([18f33f0](https://github.com/Conduit-official/Conduit/commit/18f33f0), [cfa8eb8](https://github.com/Conduit-official/Conduit/commit/cfa8eb8), [d4ccfdb](https://github.com/Conduit-official/Conduit/commit/d4ccfdb), [9d3158e](https://github.com/Conduit-official/Conduit/commit/9d3158e))
- **Browser pane wedge** — Fixed nested message pump; per-model context windows; real harness model in meter/cost ([8ee0742](https://github.com/Conduit-official/Conduit/commit/8ee0742))
- **Harness subagent visibility** — Recognize Claude Code's Agent tool (renamed from Task); permission modes actually apply; harness questions get answerable cards ([08aade6](https://github.com/Conduit-official/Conduit/commit/08aade6), [8178ea6](https://github.com/Conduit-official/Conduit/commit/8178ea6))
- **Agent cursor/typing visuals** — Now appear on every browser page ([d509d11](https://github.com/Conduit-official/Conduit/commit/d509d11))
- **Inline subagent chips** — Render correctly during parallel fan-out; click opens one agent pane ([91da3e2](https://github.com/Conduit-official/Conduit/commit/91da3e2))
- **Queue notch polish** — One container, compact inline edit, pointer-based reorder ([2d81808](https://github.com/Conduit-official/Conduit/commit/2d81808))
- **Subagent research stack** — Subagents get the full read-side research stack; proxy-proof fetch guards ([d342804](https://github.com/Conduit-official/Conduit/commit/d342804))
- **Stale turn timer** — Background-chat spinners, harness mode reset, camelCase tool args ([e109475](https://github.com/Conduit-official/Conduit/commit/e109475))
- **Web app preview** — Models can now open/preview built web apps in the in-app browser ([75dc0a6](https://github.com/Conduit-official/Conduit/commit/75dc0a6))
- **TypeScript fallbacks** — Chat-store fallbacks clear 48 `TS18046 unknown` errors ([c6f4e11](https://github.com/Conduit-official/Conduit/commit/c6f4e11))
- **Metrics HUD accuracy** — Provider-aware cache rate, decode tok/s, request-anchored TTFT, marker-free token counts, live IN/CACHE display ([9df118b](https://github.com/Conduit-official/Conduit/commit/9df118b))
- **Artifact gallery cards** — Uniform heights; agent-gated pane auto-open ([76d023b](https://github.com/Conduit-official/Conduit/commit/76d023b))
- **Split/tool panel** — Global tool panel (no docking); git rail only in the focused half; ✕-only split close ([95a3a74](https://github.com/Conduit-official/Conduit/commit/95a3a74))
- **Folded/edit row buttons** — Kill global button skin; rim shadow + hover background ([52e50cf](https://github.com/Conduit-official/Conduit/commit/52e50cf))
- **Git file spinners** — Spinner on scope switch; Send PR/Review-all scoped to focused chat with real feedback ([487ec66](https://github.com/Conduit-official/Conduit/commit/487ec66), [eb483ff](https://github.com/Conduit-official/Conduit/commit/eb483ff))
- **Docx preview overflow** — Scale pages to fit pane width (fit-to-width) ([0e951c4](https://github.com/Conduit-official/Conduit/commit/0e951c4))

### Changed
- **Welcome screen** — Time-aware greeting with icon cards; 500k context ceiling for cloud/harness meter ([2e85de8](https://github.com/Conduit-official/Conduit/commit/2e85de8))
- **GitHub pull requests** — GitHub API routes through git's proxy config ([bc605d0](https://github.com/Conduit-official/Conduit/commit/bc605d0))
- **Chat message text** — Dimmed with new `--chat-text` token, scoped to bubbles ([fad62ea](https://github.com/Conduit-official/Conduit/commit/fad62ea))
- **Git file view** — Compact diff gutters, liquid-glass filter menu, loading spinners ([eb483ff](https://github.com/Conduit-official/Conduit/commit/eb483ff))
- **Session menu** — Uses the composer's liquid glass (true-transparent body, blur24/sat160, 16px) ([b4ea2cc](https://github.com/Conduit-official/Conduit/commit/b4ea2cc))
- **Browser pane** — Edge-to-edge layout, no glass-card inset; page owns the panel like docs/html ([e255dd3](https://github.com/Conduit-official/Conduit/commit/e255dd3))
- **UI animations** — Smooth expand/collapse animations matching the tool-panel slide ([3c1aa27](https://github.com/Conduit-official/Conduit/commit/3c1aa27))

---

## [0.4.1] — 2026-08-17
- **Full-fidelity document pipeline (doc/ppt/pdf)** — New generation + preview engines across every document format:
  - **PDF generation via a real browser engine** — `generate_document` for pdf now takes `language: "html"` (default): the model authors styled HTML, which renders in a hidden WebView2 window with the Paged.js polyfill (real `@page` margin boxes, page numbers, running headers) and prints with `ICoreWebView2_7::PrintToPdf` — full CSS/SVG/Unicode/CJK support, replacing the hand-rolled Latin-1-only PDF writer
  - **DOCX generation via the `docx` npm library** (default `language: "javascript"`) — the model's program runs in a sandboxed iframe (`DocCodeRunner`) with the library preloaded, delivering real editable OOXML (headings, tables, numbering, styles) with no Python dependency; Python/`conduit_docgen` remains as fallback
  - **PPTX generation via PptxGenJS** (default `language: "javascript"`) — 16:9 layouts, native charts, slide masters, with the documented gotchas (hex colors, explicit layout) encoded in the prompts/skills
  - **PDF.js viewer** — in-app PDF pane rebuilt on pdf.js (page nav, zoom, text search, text selection), identical on WebView2/WKWebView/WebKitGTK, replacing the native `<embed>` whose behavior varied with the Evergreen runtime (and doesn't exist on Linux)
  - **docx-preview viewer** — DOCX previews render with real document styles, headers/footers, numbering and images via docx-preview, falling back to the backend HTML converter on parse failure; new "PDF view" toggle converts the original file through LibreOffice for true pagination (`office_accurate_pdf`)
  - Built-in `/docx`, `/pptx`, `/pdf` skills rewritten for the new engines
- **Relay rebrand** — User-visible product name switched from "Conduit" to "Relay" (window title, `package.json` name, `tauri.conf.json` `productName`, `<title>`, sidebar/banner/HTML strings, frost fix for toolbar popovers) ([e9abc7c3](https://github.com/Conduit-official/Conduit/commit/e9abc7c3))
- **Recognizable app icons + branded installer** — New app icon set, branded installer artwork, standalone YouTube consent card ([03c2ab8a](https://github.com/Conduit-official/Conduit/commit/03c2ab8a))
- **Git Graph commit table** — Drop the branch list, roomier rows, clip descriptions ([d834ad70](https://github.com/Conduit-official/Conduit/commit/d834ad70), [3791b529](https://github.com/Conduit-official/Conduit/commit/3791b529))
- **Floating composer over transcript** — Composer floats over the transcript; messages scroll behind the glass, edge reservations, transparent app logo ([bd69c970](https://github.com/Conduit-official/Conduit/commit/bd69c970), [f772979b](https://github.com/Conduit-official/Conduit/commit/f772979b), [2816f9da](https://github.com/Conduit-official/Conduit/commit/2816f9da), [a6088241](https://github.com/Conduit-official/Conduit/commit/a6088241))
- **Liquid glass across the app** — Composer glass extended to sidebar, modals, QR frame ([86588979](https://github.com/Conduit-official/Conduit/commit/86588979), [09449738](https://github.com/Conduit-official/Conduit/commit/09449738), [3ef47dca](https://github.com/Conduit-official/Conduit/commit/3ef47dca), [20b3c207](https://github.com/Conduit-official/Conduit/commit/20b3c207))
- **Git sidebar enhancements** — Collapsible sections, liquid glass, chat-bound data ([11837617](https://github.com/Conduit-official/Conduit/commit/11837617))
- **Right tool panel as a slide-out** — Tool panel slides open/closed like the sidebar ([a6a2ccdb](https://github.com/Conduit-official/Conduit/commit/a6a2ccdb))
- **Mode-colored permission chip** — Permission chip tinted by current mode ([f2c5af11](https://github.com/Conduit-official/Conduit/commit/f2c5af11))
- **YouTube connector** — Standalone YouTube card in the connectors panel ([f2c5af11](https://github.com/Conduit-official/Conduit/commit/f2c5af11))
- **Toolbar as title bar** — Slimmer title bar; Conduit + nav arrows live only in the sidebar ([85bcd924](https://github.com/Conduit-official/Conduit/commit/85bcd924))
- **GitHub-notch popover** — Glass on the wrapper, left-anchored under the chip ([573582b4](https://github.com/Conduit-official/Conduit/commit/573582b4))
- **Mermaid stateLabelColor fix** — state/flow node labels visible ([55740144](https://github.com/Conduit-official/Conduit/commit/55740144))
- **Composer chip with provider icon** — Chip shows the provider icon, not the provider name ([1ac84047](https://github.com/Conduit-official/Conduit/commit/1ac84047))
- **Agent/Model picker redesign** — Combined agent/model selection with icon rail and per-model local runtime settings ([cb7d782a](https://github.com/Conduit-official/Conduit/commit/cb7d782a))
- **Conversational artifacts** — Artifacts now support conversational context and can be referenced in chat ([045e9a9d](https://github.com/Conduit-official/Conduit/commit/045e9a9d))
- **MCP server gallery** — Built-in chat can now launch stdio MCP servers with one click ([b2c3d8ab](https://github.com/Conduit-official/Conduit/commit/b2c3d8ab))
- **AI diff review quick action** — Per-file and whole-tree AI diff review cards in the Git tools sidebar ([52e76ddd](https://github.com/Conduit-official/Conduit/commit/52e76ddd))
- **Per-turn RAG auto-retrieval** — Chat automatically retrieves relevant documents per turn; support for per-chat doc attachments and MCP `search_docs` ([a0635298](https://github.com/Conduit-official/Conduit/commit/a0635298))
- **Activity strip** — §3.1.6 activity strip in GitToolsSidebar ([ed35499b](https://github.com/Conduit-official/Conduit/commit/ed35499b))
- **Automation hardening** — Dual permission policies and settings improvements ([887d3364](https://github.com/Conduit-official/Conduit/commit/887d3364))
- **Mobile remote access** — E2E relay encryption (HKDF+XChaCha20-Poly1305) and Tailscale auto-serve with QR pairing ([9aed4a84](https://github.com/Conduit-official/Conduit/commit/9aed4a84), [03361f16](https://github.com/Conduit-official/Conduit/commit/03361f16), [aa7b3e4b](https://github.com/Conduit-official/Conduit/commit/aa7b3e4b))
- **Browser devtools** — New `browser_open_devtools` command to open native devtools for a browser tab
- **Conduit bundle wiring** — Interactive PTY panes now integrate the Conduit bundle ([6aae1759](https://github.com/Conduit-official/Conduit/commit/6aae1759))

### Changed
- **Chat UI** — Replaced per-message Save As / Find & Update chips with natural language controls ([4b38ded0](https://github.com/Conduit-official/Conduit/commit/4b38ded0))
- **Automation view** — Made responsive when tool panel opens ([246ca988](https://github.com/Conduit-official/Conduit/commit/246ca988), [d8c79312](https://github.com/Conduit-official/Conduit/commit/d8c79312))
- **Family connect flow** — YouTube connects standalone; slimmer toolbar chips ([254fa255](https://github.com/Conduit-official/Conduit/commit/254fa255))

### Fixed
- **Artifact creation** — `/create` now works across all providers, harness CLIs, and local models ([47ba0e86](https://github.com/Conduit-official/Conduit/commit/47ba0e86))
- **Permission policy** — Full Auto mode no longer asks for every shell command or in-roots delete ([ea4e0a96](https://github.com/Conduit-official/Conduit/commit/ea4e0a96))
- **Automation view responsiveness** — Fixed tool panel open/close behavior ([d8c79312](https://github.com/Conduit-official/Conduit/commit/d8c79312), [246ca988](https://github.com/Conduit-official/Conduit/commit/246ca988))
- **Bug audit fixes** — 26 bug and edge-case fixes from full-project audit ([c79a9e7a](https://github.com/Conduit-official/Conduit/commit/c79a9e7a))
- **Remote access binding** — Fixed relay binding to tailnet IP so phone can connect cross-network without HTTPS serve ([760bdffc](https://github.com/Conduit-official/Conduit/commit/760bdffc))
- **Remote portal QR modal** — Centered the sidebar pairing QR modal on screen ([57ca347b](https://github.com/Conduit-official/Conduit/commit/57ca347b))
- **Async serve-enable** — Added activation check for remote portal ([d9794a82](https://github.com/Conduit-official/Conduit/commit/d9794a82))
- **Family connect 400** — YouTube connects standalone; keep YouTube out of the combined Google consent ([59e2ea7d](https://github.com/Conduit-official/Conduit/commit/59e2ea7d), [d73a6d64](https://github.com/Conduit-official/Conduit/commit/d73a6d64))

### Documentation & Maintenance
- **2026-08-27 doc pass** — `PROJECT_OVERVIEW.md`, `BUG_AUDIT.md`, `PERFORMANCE_AUDIT.md`, `docs/ai-context/AI_CONTEXT.md`, and `README.md` (new) rewritten to match the current implementation. Realized metrics: 235 IPC commands, 21 tables, 68 vitest files / 460 tests passing, 539 cargo-lib tests + 1 failed, 34 `tsc --noEmit` errors, entry chunk 458.96 KB / 141.47 KB gzip. See `BUG_AUDIT.md` for the two open Sev M items.
- **Earlier (2026-08-23) doc pass** — Updated `PROJECT_OVERVIEW.md` with current architecture and metrics — **superseded by the 2026-08-27 pass above** (the 2026-08-23 numbers no longer match the code).

---

## [0.4.1] — 2026-08-17

### Added
- **Full project audit** — 26 bug and edge-case fixes surfaced from a full codebase audit ([c79a9e7a](https://github.com/Conduit-official/Conduit/commit/c79a9e7a))

### Fixed
- **Bug audit issues** — Resolved issues across browser event paths, stream buffering, usage sync, and session id collisions

---

## [0.4.0] — 2026-08-14

### Added
- **Mobile companion app** — React Native/Expo app with E2E encrypted relay and QR pairing ([aa7b3e4b](https://github.com/Conduit-official/Conduit/commit/aa7b3e4b), [9aed4a84](https://github.com/Conduit-official/Conduit/commit/9aed4a84))
- **Remote access** — Tailscale auto-serve and portal QR modal ([03361f16](https://github.com/Conduit-official/Conduit/commit/03361f16), [57ca347b](https://github.com/Conduit-official/Conduit/commit/57ca347b), [d9794a82](https://github.com/Conduit-official/Conduit/commit/d9794a82))
- **RAG integration** — Per-turn auto-retrieval and per-chat document attachment ([a0635298](https://github.com/Conduit-workspace/Conduit/commit/a0635298))
- **AI diff review** — Quick action for per-file and whole-tree AI diff review cards ([52e76ddd](https://github.com/Conduit-official/Conduit/commit/52e76ddd))
- **Activity strip** — §3.1.6 activity strip in GitToolsSidebar ([ed35499b](https://github.com/Conduit-official/Conduit/commit/ed35499b))

### Changed
- **Chat streaming** — Improved token batching and UI flushing

### Fixed
- **Automation view** — Made responsive when tool panel opens ([246ca988](https://github.com/Conduit-official/Conduit/commit/246ca988))

---

## [0.3.x] — 2026-07 (series)

### Added
- **Permission policies** — Dual policy modes (Full Auto vs Ask) and settings improvements ([887d3364](https://github.com/Conduit-official/Conduit/commit/887d3364))
- **MCP gallery** — Built-in chat MCP server gallery with one-click install ([b2c3d8ab](https://github.com/Conduit-official/Conduit/commit/b2c3d8ab))

### Fixed
- **Shell permission prompts** — Full Auto no longer asks for every shell command ([ea4e0a96](https://github.com/Conduit-official/Conduit/commit/ea4e0a96))

---

## [0.2.x] — 2026-06 (series)

### Added
- **Artifact conversational support** — Artifacts now carry conversational context ([045e9a9d](https://github.com/Conduit-official/Conduit/commit/045e9a9d))
- **Conduit bundle integration** — Interactive PTY panes wire the Conduit bundle ([6aae1759](https://github.com/Conduit-official/Conduit/commit/6aae1759))

### Changed
- **Chat UI** — Replaced per-message Save As / Find & Update chips with natural language ([4b38ded0](https://github.com/Conduit-official/Conduit/commit/4b38ded0))

### Fixed
- **Artifact creation** — `/create` now works across all providers and local models ([47ba0e86](https://github.com/Conduit-official/Conduit/commit/47ba0e86))

---

## [0.1.x] — 2026-05 (series)

### Added
- Initial desktop shell with Tauri, multi-pane PTY, browser panes, chat, git sidebar, local models, automations, and cost dashboard.

---

**Legend:**
- 🆕 = New feature
- 🔧 = Changed behavior
- 🐛 = Bug fix
- 📚 = Documentation / maintenance
