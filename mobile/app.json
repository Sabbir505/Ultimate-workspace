{
  "expo": {
    "name": "Relay Mobile",
    "slug": "relay-mobile",
    "version": "1.0.0",
    "orientation": "portrait",
    "icon": "./assets/icon.png",
    "userInterfaceStyle": "light",
    "plugins": [
      [
        "expo-camera",
        {
          "cameraPermission": "Relay needs camera access to scan the desktop pairing QR code.",
          "microphonePermission": false
        }
      ],
      [
        "expo-document-picker",
        {
          "iCloudContainerEnvironment": "Production"
        }
      ]
    ],
    "ios": {
      "supportsTablet": true,
      "bundleIdentifier": "com.relay.mobile",
      "infoPlist": {
        "NSCameraUsageDescription": "Relay needs camera access to scan the desktop pairing QR code.",
        "UIBackgroundModes": ["remote-notification"]
      }
    },
    "android": {
      "package": "com.relay.mobile",
      "permissions": ["android.permission.CAMERA"],
      "adaptiveIcon": {
        "backgroundColor": "#FAF7F5"
      }
    },
    "web": {
      "favicon": "./assets/icon.png"
    }
  }
}
